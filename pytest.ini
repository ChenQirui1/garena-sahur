[pytest]
# Owned suites only: backend/router/tests is Elson & Daniel's and still scaffolding.
testpaths = backend/ingestion backend/orchestration
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q
